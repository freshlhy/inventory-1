<%= form_for(@manufacturer) do |f| %>
  <%= content_tag :ul, :data => {:role => 'listview', :inset => true} do %>
  
    <% if @manufacturer.errors.any? %>
      <li id="error_explanation" class="ui-body-e">
        <div data-role="collapsible" data-theme="e">
        <h3><%= pluralize(@manufacturer.errors.count, "error") %> prohibited this manufacturer from being saved:</h3>
        <ul>
        <% @manufacturer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </li>
    <% end %>

    <li data-role="fieldcontain">

      <%= f.label :name %>
      <%= f.text_field :name %>
    </li>
    <li data-role="fieldcontain">
      <%= f.submit %>
    </li>
  <% end %>
<% end %>
