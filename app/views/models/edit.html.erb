<h2>Editing model</h2>

<%= render 'form' %>

<% content_for :footer do %>
  <%= content_tag :h3, link_to('Back', manufacturer_model_path(@manufacturer, @model), :data => {:role => "button", :icon => "back"}) %>
<% end %>
